// openai-types.d.ts
export type ThreadMessage = {
    // Define the properties based on your needs
  };
  
  export type ThreadMessagesPage = {
    // Define the properties based on your needs
  };
  
  export type Run = {
    id: string;
    status: string;
    // Add other properties based on the Run object
  };
  